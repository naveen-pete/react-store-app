import React from 'react';

import AppAlert from '../app-alert';

const Customers = props => {
  return (
    <div>
      <h3>Customers</h3>
      <AppAlert
        type="info"
        message="Follow Products module and implement this module."
      />
    </div>
  );
};

export default Customers;
